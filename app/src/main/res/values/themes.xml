<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- This app has no XML Material Components/AppCompat dependency (Compose-only, themed via
         Compose Material3 at the Kotlin level) — the manifest theme just needs a platform base
         for the launch window before Compose content sets in. -->
    <style name="Theme.FuelPrices" parent="android:Theme.Material.Light.NoActionBar">
    </style>
</resources>
